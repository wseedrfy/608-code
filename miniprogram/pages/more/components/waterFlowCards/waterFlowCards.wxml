<scroll-view  scroll-y="true"  style="height:{{windowHeight}}px; background-color: rgba(153, 153, 153, 0.11);" bindscrolltolower="testFnc" lower-threshold="50" bindscrolltoupper="onPullDownRefresh" upper-threshold="-30" enable-back-to-top="{{true}}" fast-deceleration="true" paging-enabled="true" class="page" style="opacity: {{list[0] ? 1 : 0}};">
    <!-- height:{{windowHeight}} -->
    <view class='content'>
      <view class="left">
        <view wx:for="{{leftList}}" wx:key="leftList">
          <!-- <loveCards wx:if="{{item.Title == '闲聊'}}" item="{{item}}" type="left" bind:ShowContent="getBackData" openusername="{{openusername}}"></loveCards> -->
          <campusCards  item="{{item}}" type="left" openusername="{{openusername}}" />
        </view>
      </view>

      <view class="right">
        <view wx:for="{{rightList}}" wx:key="rightList">
          <campusCards item="{{item}}" type="right" openusername="{{openusername}}" />
        </view>
      </view>

    </view>
    <!-- 边界处理 -->
    <view class="DataNull" wx:if="{{loadAll}}">-无更多内容-</view>
</scroll-view>